# Dockerfile for SynoCommerce Background Async Workers
FROM node:22-alpine AS base
WORKDIR /app
COPY package*.json tsconfig.json ./
RUN npm ci
COPY . .

CMD ["node", "--import", "tsx", "src/delivery/workers.ts"]
