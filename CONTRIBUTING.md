# Contributing to SynoCommerce

Thank you for contributing to SynoCommerce!

## Pull Request Guidelines

1. Ensure zero TypeScript compilation errors: `npm run typecheck`.
2. Ensure 100% test pass execution: `npm run test`.
3. Follow conventional commits e.g. `feat(module): description` or `fix(iam): description`.
