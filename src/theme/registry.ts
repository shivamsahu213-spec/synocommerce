import { defaultBrand } from '@theme/brands/default.brand';
import { coreTheme } from '@theme/themes/core.theme';

export const brandRegistry = {
  [defaultBrand.code]: defaultBrand
};

export const themeRegistry = {
  [coreTheme.code]: coreTheme
};
