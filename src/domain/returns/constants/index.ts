/**
 * Returns Domain Constants
 *
 * @module domain/returns/constants
 */

import { ReturnDisposition } from '../types';

export const DEFAULT_RETURN_WINDOW_DAYS = 30;

export const DEFAULT_RETURN_DISPOSITION: ReturnDisposition = 'RESTOCK';
