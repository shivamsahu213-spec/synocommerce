// src/database/utils/queryBuilder.ts
/**
 * Utility wrapper around the filter builder.
 * Re‑exports the `buildWhere` function for repository consumption.
 */
import { buildWhere } from '../filters';

export { buildWhere };
