# Dockerfile for SynoCommerce API Server
FROM node:22-alpine AS base
WORKDIR /app
COPY package*.json tsconfig.json ./
RUN npm ci
COPY . .

EXPOSE 3000
CMD ["node", "--import", "tsx", "src/delivery/index.ts"]
