# Elasticsearch Enterprise Search Integration

## Overview

Elasticsearch integration for high-scale document indexing, complex aggregation facets, and distributed search clusters.

---

## Configuration

Set environment variables in `.env.production`:

```env
ELASTICSEARCH_NODE=https://es-cluster.internal.synocommerce.com:9200
ELASTICSEARCH_API_KEY=elastic_prod_key
```
