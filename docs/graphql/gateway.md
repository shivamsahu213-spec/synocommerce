# GraphQL Router & Query Planner

## Overview

High-throughput GraphQL router performing query planning, sub-request batching, and Automatic Persisted Queries (APQ) caching.

---

## APQ Caching

Clients send SHA-256 hashes instead of full query strings to reduce bandwidth by up to 90%.
