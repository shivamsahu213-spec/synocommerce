# AWS CloudWatch Logs & Metrics Integration

## Overview

AWS CloudWatch integration for container logs aggregation, CloudWatch Alarm routing, and EC2/ECS metrics.

---

## Configuration

Set environment variables in `.env.production`:

```env
AWS_CLOUDWATCH_LOG_GROUP=/synocommerce/production
AWS_CLOUDWATCH_LOG_STREAM=app-stream
```
