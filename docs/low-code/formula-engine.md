# Formula Engine & Expression Syntax

## Overview

Excel and Salesforce Flow-like expression engine for calculated fields, validation rules, and dynamic section visibility.

---

## Supported Formula Functions

- `SUM(...)`: Sums numerical inputs.
- `IF(cond, trueVal, falseVal)`: Conditional branching.
- `CONCAT(str1, str2)`: String concatenation.
