# Azure Blob Storage Integration

## Overview

Azure Blob Storage integration for enterprise cloud storage with immutability and lifecycle management.

---

## Configuration

Set environment variables in `.env.production`:

```env
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=synoprodstorage;AccountKey=prod_key;EndpointSuffix=core.windows.net
```
