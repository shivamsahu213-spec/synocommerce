# Migration Security, Encryption & SHA-256 Checksums

## Overview

Security protocols enforcing encrypted import file handling, SHA-256 payload integrity checksums, multi-tenant data isolation, and audit logging.

---

## Security Features

- **SHA-256 Checksum**: Rejects corrupted or tampered import packages.
- **Tenant Isolation**: Restricts data imports to the active store tenant.
