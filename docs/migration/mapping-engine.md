# Data Mapping Engine & Transformation Rules

## Overview

Field-level mapping specification connecting external source attributes (e.g., `product_title`) to SynoCommerce domain models (`name`).

---

## Features

- Custom JS transformation scripts.
- Default value fallbacks.
- SKU/Email duplicate detection.
