# SendGrid Email Integration

## Overview

SendGrid v3 Mail Send API integration with dynamic template engine support.

---

## Configuration

Set environment variables in `.env.production`:

```env
SENDGRID_API_KEY=SG.prod_your_api_key
```
